import { addPieChart } from '../add-chart.js'
(async function () {
  const data = [
  ];
  addPieChart('season25Pie', [data], { plugins: { title: { display: true, text: 'Coeur et âme (25) - A venir en 2025' } } });
})();
