import { addPieChart } from '../add-chart.js'
(async function () {
  const data = [
  ];
  addPieChart('season29Pie', [data], { plugins: { title: { display: true, text: 'Kaléidoscopes (29) ' } } });
})();
